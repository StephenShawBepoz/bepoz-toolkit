<UserControl x:Class="BepozToolkit.App.Views.LogsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:converters="clr-namespace:BepozToolkit.App.Converters"
             xmlns:vm="clr-namespace:BepozToolkit.App.ViewModels"
             d:DataContext="{d:DesignInstance vm:LogsViewModel}"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             Background="{DynamicResource BackgroundPrimary}">

    <UserControl.Resources>
        <converters:BoolToVisibilityConverter x:Key="BoolToVisibility" />
        <converters:LogLevelToBrushConverter x:Key="LogLevelToBrush" />
        <converters:DurationConverter x:Key="DurationConverter" />
    </UserControl.Resources>

    <DockPanel Margin="24">
        <!-- Loading Bar -->
        <ProgressBar DockPanel.Dock="Top" IsIndeterminate="True" Height="2" Margin="0,0,0,8"
                     Visibility="{Binding IsLoading, Converter={StaticResource BoolToVisibility}}" />

        <!-- Filter Bar -->
        <Border DockPanel.Dock="Top" Background="{DynamicResource CardBackground}"
                BorderBrush="{DynamicResource CardBorder}" BorderThickness="1"
                CornerRadius="8" Padding="16,12" Margin="0,0,0,16">
            <Border.Effect>
                <DropShadowEffect ShadowDepth="1" BlurRadius="4" Opacity="0.06" />
            </Border.Effect>
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <!-- Level Filter Buttons -->
                <ListBox Grid.Column="0" ItemsSource="{Binding Levels}"
                         SelectedItem="{Binding SelectedLevel}"
                         Background="Transparent" BorderThickness="0"
                         ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                    <ListBox.ItemsPanel>
                        <ItemsPanelTemplate>
                            <StackPanel Orientation="Horizontal" />
                        </ItemsPanelTemplate>
                    </ListBox.ItemsPanel>
                    <ListBox.ItemContainerStyle>
                        <Style TargetType="ListBoxItem">
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="ListBoxItem">
                                        <Border x:Name="LevelBorder" CornerRadius="4" Padding="10,5"
                                                Margin="0,0,4,0" Cursor="Hand"
                                                Background="{DynamicResource BackgroundTertiary}"
                                                BorderBrush="{DynamicResource CardBorder}"
                                                BorderThickness="1">
                                            <TextBlock x:Name="LevelText" Text="{Binding}"
                                                       FontSize="12" FontWeight="Medium"
                                                       Foreground="{DynamicResource TextSecondary}" />
                                        </Border>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsSelected" Value="True">
                                                <Setter TargetName="LevelBorder" Property="Background"
                                                        Value="{DynamicResource AccentPrimary}" />
                                                <Setter TargetName="LevelBorder" Property="BorderBrush"
                                                        Value="{DynamicResource AccentPrimary}" />
                                                <Setter TargetName="LevelText" Property="Foreground"
                                                        Value="{DynamicResource TextOnAccent}" />
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </ListBox.ItemContainerStyle>
                </ListBox>

                <!-- Machine Dropdown -->
                <TextBlock Grid.Column="1" Text="Machine:" FontSize="12"
                           Foreground="{DynamicResource TextSecondary}"
                           VerticalAlignment="Center" Margin="16,0,8,0" />
                <ComboBox Grid.Column="2" ItemsSource="{Binding Machines}"
                          SelectedItem="{Binding SelectedMachine}"
                          MinWidth="140" FontSize="12" VerticalContentAlignment="Center" />
            </Grid>
        </Border>

        <!-- Refresh Button -->
        <DockPanel DockPanel.Dock="Top" Margin="0,0,0,8">
            <Button DockPanel.Dock="Right" Content="Refresh" Padding="12,6"
                    Command="{Binding RefreshCommand}"
                    Background="{DynamicResource AccentPrimary}"
                    Foreground="{DynamicResource TextOnAccent}"
                    BorderThickness="0" Cursor="Hand" FontSize="12">
                <Button.Template>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="6" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center" />
                        </Border>
                    </ControlTemplate>
                </Button.Template>
            </Button>
            <TextBlock Text="{Binding LogEntries.Count, StringFormat='{}{0} entries'}"
                       FontSize="12" Foreground="{DynamicResource TextSecondary}"
                       VerticalAlignment="Center" />
        </DockPanel>

        <!-- Log Entries DataGrid -->
        <Border Background="{DynamicResource CardBackground}"
                BorderBrush="{DynamicResource CardBorder}" BorderThickness="1"
                CornerRadius="8" Padding="0" ClipToBounds="True">
            <ListView ItemsSource="{Binding LogEntries}"
                      BorderThickness="0" Background="Transparent"
                      ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                      ScrollViewer.VerticalScrollBarVisibility="Auto">
                <ListView.ItemContainerStyle>
                    <Style TargetType="ListViewItem">
                        <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                        <Setter Property="Padding" Value="16,10" />
                        <Setter Property="BorderBrush" Value="{DynamicResource DividerColor}" />
                        <Setter Property="BorderThickness" Value="0,0,0,1" />
                        <Setter Property="Background" Value="Transparent" />
                    </Style>
                </ListView.ItemContainerStyle>
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="150" />
                                <ColumnDefinition Width="70" />
                                <ColumnDefinition Width="140" />
                                <ColumnDefinition Width="80" />
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>

                            <!-- Timestamp -->
                            <TextBlock Grid.Column="0" Text="{Binding TimestampFormatted}"
                                       FontSize="12" FontFamily="Consolas"
                                       Foreground="{DynamicResource TextSecondary}"
                                       VerticalAlignment="Center" />

                            <!-- Level (color-coded) -->
                            <Border Grid.Column="1" CornerRadius="3" Padding="6,2"
                                    HorizontalAlignment="Left" VerticalAlignment="Center"
                                    Margin="8,0,0,0">
                                <Border.Background>
                                    <SolidColorBrush Color="Transparent" />
                                </Border.Background>
                                <TextBlock Text="{Binding Level}" FontSize="11" FontWeight="SemiBold"
                                           Foreground="{Binding Level, Converter={StaticResource LogLevelToBrush}}" />
                            </Border>

                            <!-- Tool Name -->
                            <TextBlock Grid.Column="2" Text="{Binding ToolName}"
                                       FontSize="12" FontWeight="Medium"
                                       Foreground="{DynamicResource TextPrimary}"
                                       VerticalAlignment="Center" Margin="8,0,0,0"
                                       TextTrimming="CharacterEllipsis" />

                            <!-- Duration -->
                            <TextBlock Grid.Column="3" FontSize="12" FontFamily="Consolas"
                                       Foreground="{DynamicResource TextSecondary}"
                                       VerticalAlignment="Center" Margin="8,0,0,0"
                                       Text="{Binding DurationMs, Converter={StaticResource DurationConverter}}" />

                            <!-- Message -->
                            <TextBlock Grid.Column="4" Text="{Binding Message}"
                                       FontSize="12" Foreground="{DynamicResource TextSecondary}"
                                       VerticalAlignment="Center" Margin="8,0,0,0"
                                       TextTrimming="CharacterEllipsis" />

                            <!-- View Output Button -->
                            <Button Grid.Column="5" Content="View" Padding="8,4"
                                    FontSize="11" Cursor="Hand"
                                    Command="{Binding DataContext.ViewOutputCommand,
                                        RelativeSource={RelativeSource AncestorType=UserControl}}"
                                    CommandParameter="{Binding}"
                                    Background="Transparent"
                                    Foreground="{DynamicResource AccentPrimary}"
                                    BorderThickness="0" VerticalAlignment="Center" />
                        </Grid>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </Border>
    </DockPanel>
</UserControl>
